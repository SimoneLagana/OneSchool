<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag "profile.css" %>
    <%= javascript_include_tag('student.js') %>
    <title>Profile</title>
</head>
<body>
    <div id="popupcontainer"></div>
    <nav class="navbar">
        <%= link_to student_home_url(CF: params[:CF]) do %>
            <label class="writ">
                <%= @student.name %>
            </label>
        <% end %>
        <%= link_to student_profile_url(CF: params[:CF]) do %>
            <div class="profile">
                Profile
            </div>
        <% end %>
      </nav>
    <div class="home">
        <div class="main-panel">
            <label class="profile-field">Name: <%= @student.name %> </label>
            <label class="profile-field">Surname: <%= @student.surname %> </label>
            <label class="profile-field">CF: <%= @student.CF %> </label>
            <label class="profile-field">Email: <%= @student.mail %> </label>
            <label class="profile-field">School code: <%= @student.school_code %> </label>
            <div class="button-container">
                <button class="style-button" onclick="editpassword('<%= @student.CF%>', '<%= @student.mail%>')">edit password</button>
                <%= form_tag student_checklogout_path, method: :delete do %>
                    <%= submit_tag 'Logout', class: 'style-button' %> 
                <% end %> 
            </div>
            
        </div>
    </div>
</body>
</html>