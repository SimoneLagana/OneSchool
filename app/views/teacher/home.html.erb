<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="teacher_home.css">
    <title>Home</title>
</head>
<body>
    <nav class="navbar">
        <label class="writ">
          <%= @teacher.name %>
        </label>
        <%= link_to teacher_profile_url(CF: params[:CF]) do %>
            <div class="profile">
                Profile
            </div>
        <% end %>
         
      </nav>
    <div class="home">
        <div class="whole-panel">
            <div class="right-panel">
                <%= link_to "/teacher/calendar", class: "calendar" do %>
                    <div class="calendar">
                        Agenda personale
                    </div>
                <% end %>
                <%= link_to teacher_meeting_url(CF: params[:CF]), class: "meeting" do %>
                    <div class="meeting">
                        Link meeting
                    </div>
                <% end %>
            </div>
            <div class="left-panel">
                <label class="label-panel"> </label>
                <%= form_with url: teacher_classroom_url, class: 'form-class', method: :get do |form| %>
                    <label class="label-class">Seleziona una classe </label>
                    <%= form.select :classroom, options_for_select(@classrooms), {},  class: "select-class"%>
                    <%= form.hidden_field :CF, value: @teacher.CF %>
                    <%= form.submit "vai alla classe", class: "submit-button" %>
                    <% end %>

                
            </div>
        </div>
    </div>
</body>
</html>